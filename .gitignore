# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# dev credentials
.dev-credentials.md

# Personal / local notes (not for the repo)
FORME.md

# Backend Lambda source (local copy; not committed — deploy from your machine)
/lambda functions/

# Ad-hoc local Lambda / backend copies (keep repo Next.js–only)
/ID-Pirate-CreateOrder-Function/
/admin_handler/
/idPirateOrderLookup/
/idPirate_auth/
idpirate.com.txt

# Lambda layer build output (see scripts/build-pyjwt-lambda-layer.sh)
/pyjwt-layer.zip
.vscode/launch.json
